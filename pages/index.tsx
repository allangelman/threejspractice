import Head from "next/head";
import { Canvas } from "@react-three/fiber";
import { Geometry } from "../components/Geo";
import { useRef, useState } from "react";
import { MatCapButton } from "../components/MatCapButton";
import { Vector3, LoadingManager } from "three";
import cn from "classnames";
import styles from "../styles/index.module.css";

export default function Home() {
  const [selectedMaterial, setSelectedMaterial] = useState<string>("default");

  const pointRef = useRef<HTMLDivElement>(null);

  const loadingManager = new LoadingManager(() => {});
  return (
    <>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/threejspractice/favicon.ico" />
      </Head>
      <div className="flex mx-auto mt-[20px] w-[600px] h-[600px]">
        <Canvas
          camera={{ fov: 50, position: [0, 2, 5] }}
          className="h-[800px] bg-slate-700"
        >
          <Geometry
            points={[
              {
                elementRef: pointRef,
                position: new Vector3(0.0, 0.0, 0.8),
              },
            ]}
          />
        </Canvas>
        <div
          ref={pointRef}
          className={cn(
            styles.point,
            "w-5 h-5 rounded-full absolute top-1/2 left-1/2 bg-black/50 opacity-0"
          )}
        />
      </div>
    </>
  );
}
